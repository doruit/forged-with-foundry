apiVersion: forgedwithfoundry.dev/v1alpha1
kind: AgentGovernanceContract

# Fixture demonstrating an approved DPIA, recorded through the FwF
# governance-contract schema layer. Independent of this control's Azure
# Policy demo, which evaluates deployment-request tags directly and never
# reads this file -- see the control's README for that mechanism.
metadata:
  agentId: customer-support-agent
  description: Governance contract for the customer support triage agent
  source: https://github.com/doruit/forged-with-foundry
  license: MIT

spec:
  agentRef:
    definition: ./agent.yaml

  controls:
    - id: PRI-PRE-001
      version: "1.0.0"
      evidence:
        dpiaStatus: approved
        dpiaEvidenceId: DPIA-2026-014
